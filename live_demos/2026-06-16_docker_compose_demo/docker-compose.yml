services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      GREETING: "Hello from Compose"
      APP_ENV: "compose"
      INPUT_DIR: "/mnt/input"
      OUTPUT_DIR: "/mnt/output"
    volumes:
      - ./input:/mnt/input:ro    # read-only — container cannot modify source data
      - ./output:/mnt/output     # read-write — container writes results here
